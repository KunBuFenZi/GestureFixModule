plugins {
    id 'com.android.application'
}

android {
    compileSdk 34
    namespace 'com.gesturefix'

    defaultConfig {
        applicationId "com.gesturefix"
        minSdk 31
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    signingConfigs {
        fixed {
            storeFile file('../gesturefix.jks')
            storePassword 'gesturefix123'
            keyAlias 'gesturefix'
            keyPassword 'gesturefix123'
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.fixed
        }
        release {
            minifyEnabled false
            signingConfig signingConfigs.fixed
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }

    configurations.all {
        resolutionStrategy {
            force 'com.android.tools:r8:8.2.47'
        }
    }
}

dependencies {
    compileOnly 'de.robv.android.xposed:api:82'
}
